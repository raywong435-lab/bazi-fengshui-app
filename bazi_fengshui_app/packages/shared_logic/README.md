# shared_logic

Shared Dart utilities for the AIBaZi monorepo. Place common models, helpers, and utilities here and reference via path dependencies from apps.

Example usage in a Flutter app's `pubspec.yaml`:

```yaml
dependencies:
  shared_logic:
    path: ../packages/shared_logic
```
